export default function RecommendationsPage() {
  return (
    <div style={{ padding: '1.5rem' }}>
      <div className="card">
        <h2 style={{ marginTop: 0, color: 'var(--color-text-primary)', fontFamily: 'var(--font-sans)' }}>
          Recommendations
        </h2>
        <p style={{ color: 'var(--color-text-muted)', fontFamily: 'var(--font-sans)', margin: 0 }}>
          Personalised energy-saving recommendations will appear here once the feature is available.
        </p>
      </div>
    </div>
  )
}
